services:
  - type: static
    name: polyguard-dashboard
    buildCommand: npm run build
    staticPublishDir: dist
    envVars:
      - key: NODE_VERSION
        value: "20"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
