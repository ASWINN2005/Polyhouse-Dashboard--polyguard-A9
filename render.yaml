services:
  - type: web
    name: polyguard-dashboard
    env: static
    buildCommand: npm run build
    staticPublishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
