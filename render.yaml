static_sites:
  - name: polyguard-dashboard
    buildCommand: npm run build
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: "20"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
